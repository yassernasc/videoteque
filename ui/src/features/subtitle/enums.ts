enum Style {
  Bordered,
  Shadowed,
}

enum Color {
  White,
  Yellow,
}

enum Font {
  Futura,
  Georgia,
  Sans,
}

enum PositionChange {
  Lower,
  Upper,
}

enum SizeChange {
  Bigger,
  Smaller,
}

enum OutOfSyncState {
  ABitEarly,
  ABitLate,
  TooEarly,
  TooLate,
}

export { Style, Color, Font, PositionChange, SizeChange, OutOfSyncState }
